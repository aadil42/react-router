import { Link } from 'react-router-dom';

const Products = () => {
    return (
        <>
        <h1>Products</h1>
        <Link to="/"> got to Homepage</Link>
        </>
    );
}

export default Products;